# Stock Traccar 6.14.5 with the zebrelina gl200 decoder patches. Only the main jar
# differs; /opt/traccar/lib and the bundled JRE are identical for a patch on the
# same tag, so nothing else is copied.
FROM traccar/traccar:6.14.5
COPY target/tracker-server.jar /opt/traccar/tracker-server.jar
